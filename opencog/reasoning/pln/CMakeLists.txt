# PLNModule
IF (TBB_FOUND)
	INCLUDE_DIRECTORIES (
		${TBB_INCLUDE_DIR}
	)

	LINK_DIRECTORIES(
		${TBB_INCLUDE_DIR}
		${TBB_LIBRARY_DIRS}
	)

	ADD_LIBRARY(plnmodule SHARED
		PLNModule
	)

	TARGET_LINK_LIBRARIES(plnmodule
		server
		tbb
		ruleengine
	)
ENDIF (TBB_FOUND)

ADD_LIBRARY (parse SHARED
	ParseModule.cc ParsingAgent.cc
)

INCLUDE_DIRECTORIES (
	${CSOCKETS_INCLUDE_DIRS}
	${OPENSSL_INCLUDE_DIR}
	${ODBC_INCLUDE_DIRS}
    ${LINK_GRAMMAR_INCLUDE_DIRS}
)

TARGET_LINK_LIBRARIES(parse
    ${LINK_GRAMMAR_LIBRARIES}
)

INSTALL (TARGETS parse
	LIBRARY DESTINATION "lib${LIB_DIR_SUFFIX}/opencog"
)

;
; Tools for running AIML in the AtomSpace.
;

(use-modules (opencog) (opencog nlp) (opencog nlp chatbot-eva))

; ==============================================================
(define (make-token-sequnce SENT)
"
  make-token-sequence -- Create a list of words from input sentence.

  SENT is assumed to be a SentenceNode, pointing to text that has been
  processed by RelEx and R2L.
"
)

; ==============================================================

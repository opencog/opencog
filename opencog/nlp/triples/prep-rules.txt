;
; prep-rules.txt
;
; An experimental set of copula/preposition-mangling rules. 
;
; The format of this file is supposed to be similar to, if not identical
; to the "mapping_rules.txt" file used in the Relex framing code.
;
; -----------------------------------------------------------
; The following rules create verb-preposition linkages; these must
; be created in advance, before the later rules run. These do not
; need to be precise (its OK to generate some grabage, I think(??)
; but its important that all verb-prep paris be considered
; XX perhaps a check for verb is needed ??
;
# IF _obj(be,$var1)
      ^ $prep($var1,$var2)              ; preposition
      ^ %LemmaLink($var1,$word1)        ; word of word instance
      THEN ^3_&scm:make-verb-prep($word1,$prep)

# IF _subj(be,$var1)
      ^ $prep($var1,$var2)              ; preposition
      ^ %LemmaLink($var1,$word1)        ; word of word instance
      THEN ^3_&scm:make-verb-prep($word1,$prep)

; Sentence: "The color of the sky is blue."
# IF _predadj($var1,$var0)
      ^ $prep($var1,$var2)              ; preposition 
      ^ %LemmaLink($var1,$word1)        ; word of word instance
      THEN ^3_&scm:make-verb-prep($word1,$prep)

; Sentence: "Pottery is made from clay."
# IF _obj($var0,$var1)
      ^ $prep($var0,$var2)              ; preposition 
      ^ %LemmaLink($var0,$word0)        ; word of word instance
      THEN ^3_&scm:make-verb-prep($word0,$prep)


ADD_LIBRARY (dotty SHARED
	DottyModule.cc 
)

INCLUDE_DIRECTORIES (
	${CSOCKETS_INCLUDE_DIRS}
	${OPENSSL_INCLUDE_DIR}
	${ODBC_INCLUDE_DIRS}
)

INSTALL (TARGETS dotty
	LIBRARY DESTINATION "lib${LIB_DIR_SUFFIX}/opencog"
)

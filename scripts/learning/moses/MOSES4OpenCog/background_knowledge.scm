; File containing background knowledge about features and models

; Predicate Top is the predicate which is always true
(PredicateNode "Top" (stv 1 1))

; Predicate Bottom is the predicate which is always false
(PredicateNode "Bottom" (stv 0 1))


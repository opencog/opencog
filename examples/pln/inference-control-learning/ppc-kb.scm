;; Knowledge-base about preproof and anything else we need to
;; post-process the trace atomspace before handing it to the process
;; in charge of producing inference control rules. ppc stands for
;; post-process corpus.
;;
;; Actually these are moved in there own files, namely,
;; and-bit-prior.scm, proof-is-preproof.scm and
;; preproof-expander-is-preproof to be processed individually as to
;; speed up ppc inferences.

ADD_CXXTEST(PLNRulesUTest)

LINK_DIRECTORIES(
	${PROJECT_BINARY_DIR}/opencog/atomspace
	${PROJECT_BINARY_DIR}/opencog/server
)

TARGET_LINK_LIBRARIES(PLNRulesUTest
  ${ATOMSPACE_LIBRARY}
  ${COGUTIL_LIBRARY}
  ${GUILE_LIBRARIES}
  ${ATOMSPACE_smob_LIBRARY}
  ${ATOMSPACE_query_LIBRARY}
  ${ATOMSPACE_execution_LIBRARY}
)

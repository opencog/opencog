[PLN_TEST]
# NOTEvaluatorRule has no compute method currently. It should be easy to add, but watch out for this Rule combining with itself in the BIT.
COMMENT = Test NotEvaluatorRule
LOAD = new/simple_atoms
TARGET = (NotLink a)
# Should get [0.2,0.8]
MIN_TV = [0.19,0.79]
MAX_TV = [0.21,0.81]
MAX_STEPS = 200

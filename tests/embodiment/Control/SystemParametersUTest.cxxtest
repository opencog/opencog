/**
 * SystemParametersUTest
 *
 * $Header$
 *
 * Author: Andre Senna
 * Creation: Wed Jul  4 15:05:31 BRT 2007
 */

#include <SystemParameters.h>

using namespace Control;

class SystemParametersUTest : public CxxTest::TestSuite {

    private:

    public:

    SystemParametersUTest() {

    }

    void setUp() {
    }

    void tearDown() {
    }

    void testLoad() {
        SystemParameters params;
        //printf("<%s> <%s>\n", params.get("ROUTER_ID").c_str(), params.get("ROUTER_PORT").c_str());
        //printf("%s\n", system("pwd"));
        std::string defaultRouterID = params.get("ROUTER_ID");
        std::string defaultRouterPORT = params.get("ROUTER_PORT");
        //printf("<%s> <%s>\n", defaultRouterID.c_str(), defaultRouterPORT.c_str());
        params.loadFromFile("../../../test/Control/TestParams.txt");
        //printf("<%s> <%s>\n", params.get("ROUTER_ID").c_str(), params.get("ROUTER_PORT").c_str());
        TS_ASSERT(params.get("ROUTER_ID") == "FAKE_ROUTER_ID");
        TS_ASSERT(params.get("ROUTER_PORT") == defaultRouterPORT);
    }
}; // class

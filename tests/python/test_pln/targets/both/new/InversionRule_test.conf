[PLN_TEST]
# Currently only allowed on InheritanceLink, but it should probably be allowed
# on ImplicationLink as well. It'd only be a one-line change.
COMMENT = Test InversionRule.
# It wouldn't work on new/implications, for that reason.
LOAD = new/InversionRule_demo
TARGET = inh_ba
# Gets [0.6, 0.83]
MIN_TV = [0.4,0.7]
MAX_TV = [1.01,1.01]
MAX_STEPS = 200


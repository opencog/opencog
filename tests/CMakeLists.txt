ENABLE_TESTING()
INCLUDE(AddCxxtest)

IF (WIN32)
	ADD_DEFINITIONS(-D_CRT_SECURE_CPP_OVERLOAD_STANDARD_NAMES -D_CRT_SECURE_NO_WARNINGS)
ENDIF (WIN32)
ADD_DEFINITIONS(-DOPENCOG_SOURCE_DIR=\\"${CMAKE_SOURCE_DIR}\\" -DOPENCOG_BINARY_DIR=\\"${CMAKE_BINARY_DIR}\\")

IF (CXXTEST_FOUND)
	ADD_SUBDIRECTORY (atomspace)
	ADD_SUBDIRECTORY (xml)
	ADD_SUBDIRECTORY (util)
	IF (ODBC_FOUND)
		ADD_SUBDIRECTORY (query)
	ENDIF (ODBC_FOUND)
	ADD_SUBDIRECTORY (server)
	ADD_SUBDIRECTORY (persist)
	ADD_SUBDIRECTORY (dynamics)
	ADD_SUBDIRECTORY (reasoning)
ENDIF (CXXTEST_FOUND)


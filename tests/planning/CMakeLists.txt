INCLUDE_DIRECTORIES(
    ${COGUTIL_INCLUDE_DIR}
    ${ATOMSPACE_INCLUDE_DIR}
)

LINK_DIRECTORIES(
    ${ATOMSPACE_LIBPATH}
)

ADD_CXXTEST(ActionUTest)
TARGET_LINK_LIBRARIES(ActionUTest
    planning
    ${ATOMSPACE_LIBRARIES}
    ${COGUTIL_LIBRARY}
)

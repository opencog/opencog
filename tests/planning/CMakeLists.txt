INCLUDE_DIRECTORIES(
    ${COGUTIL_INCLUDE_DIR}
    ${ATOMSPACE_INCLUDE_DIR}
)

LINK_DIRECTORIES(
    ${ATOMSPACE_LIBPATH}
)

LINK_LIBRARIES(
    planning
    ${ATOMSPACE_LIBRARIES}
    ${COGUTIL_LIBRARY}
)

ADD_CXXTEST(ActionUTest)
ADD_CXXTEST(ActionSelectorUTest)
